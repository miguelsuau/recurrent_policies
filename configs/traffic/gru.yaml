parameters:
    main:
        # Common hyperparameters
        simulator: 'global'
        env: 'traffic'
        obs_size: 30
        rollout_steps: 128
        num_actions: 2
        name: 'traffic'
        hidden_size: 128
        hidden_size_2: 128
        seq_len: 32
        num_workers: 8
        epsilon: 0.1
        beta: 0.01
        render: False
        policy: 'GRUPolicy'
        load_policy: False
        framestack: False
        # total_steps: 1.0e+6